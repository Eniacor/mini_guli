<view class="address">
    <!--view class="address_list">
        <van-radio-group value="{{default}}">
            <view class="list_item" wx:for="{{address}}" wx:key="index">
                <view class="item_left">
                    <view class="left_top">
                        <text class="top_name">收货人：{{item.name}}</text>
                        <text class="top_mobile">{{item.phone}}</text>
                    </view>
                    <view class="left_middle">
                        收货地址：{{item.province}}{{item.city}}{{item.area}}{{item.detail}}
                    </view>
                </view>
                <view class="item_middle"></view>
                <view class="item_right" bindtap="onSexClick" data-id="{{item.id}}" >
                    <van-radio name="{{item.id}}" checked-color="#333" />
                </view>
            </view>
        </van-radio-group>
    </view  -->
    <view class="empty">
        <image class="empty_img" src="/images/static/empty_address.png"></image>
        <view class="empty_txt">暂无收货地址哦～</view>
    </view>
    <navigator hover-class="none" url="/pages/address/item/index">
        <van-button class="btn" sizetype="default">新增地址</van-button>
    </navigator>
</view>